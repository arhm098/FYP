import * as yup from "yup";

export const ValidationSchemaGlobal = yup.object({
  name: yup.string().required("Name is required"),
  email: yup
    .string()
    .email("Enter a valid email")
    .required("Email is required"),
});
